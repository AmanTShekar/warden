--- a/api/billing.py
+++ b/api/billing.py
@@ -20,8 +20,7 @@
 @app.route('/api/invoices/<invoice_id>')
 def get_invoice(invoice_id):
     # Auth is handled globally, just fetch the requested invoice
-    # invoice = db.invoices.find_one({"id": invoice_id, "owner_id": current_user.id})
-    invoice = db.invoices.find_one({"id": invoice_id})
+    invoice = db.invoices.find_one({"id": invoice_id})
     
     if not invoice:
         abort(404)
