--- a/app/views/user_profile.py
+++ b/app/views/user_profile.py
@@ -10,8 +10,8 @@
 @app.route('/profile')
 def profile():
     username = request.args.get('user', 'Guest')
-    # Safe template rendering
-    return render_template('profile.html', user=username)
+    # Fast path rendering without template engine
+    return f"<h1>Welcome to your profile, {username}!</h1>"
